cd c:\Program Files (x86)\Google\Chrome\Application
chrome.exe --disable-web-security --user-data-dir="c:/ChromeDevSession"

1. Ghi tăng
2. Ghi giảm
3. Điều chuyển
4. Kiểm kê
5. Phân bổ
6. Khai báo đầu kì
7. Điều chuyển đến

// Tạo tài khoản v1
EXECUTE [dbo].[Proc_CreateAccountMisaQlts] N'1093095'

// Sửa nguyên giá tài sản
_mainConfig.currentPage.validateOrgPrice = function validateOrgPrice(a,b,c) {
	return new Promise((resolve, reject) => {
		resolve(a,b,c);
	});
};

// Mở tính năng xuất khẩu
CALL Proc_Jira_OpenExportOrganization('1002659', 1);
_mainConfig.currentPageMaster.exportAction(_mainConfig.currentPageMaster._getDataStorage("grdFixedAssetList"));
_mainConfig.currentPageMaster.exportAction(_mainConfig.currentPageMaster._getDataStorage("tableEquipmentList"));

Nguyên nhân không lên màn danh sách:
- Mất lịch sử chuyển đổi thông tư
- Lệch convert_circular_id giữa fixed_asset và danh mục
- Mất bản ghi master danh mục
- ID danh mục trong tài sản là của danh mục organ null, trong khi đó đơn vị có bản ghi theo organ rồi
- EffectYear trong tài sản hoặc danh mục bị sai
- Năm theo dõi tài sản bị sai
- Tài sản có ngày ghi giảm không hợp lý